#include <stdio.h>

int main (){

    int x;
    int y;
    int z;

    scanf("%i %i %i", &x, &y, &z);
    

    if(x < y && x < z)
		printf("%i", x);
	if(y < z && y > x)
		printf(" %i ", y);
	if(z > x && z > y)
		printf(" %i", z);
	if(x == y && x == z && z == y)
		printf( "%i, %i, %i", x, y, z);
        
    printf("\n");

return 0;
}
